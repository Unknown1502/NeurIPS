# Requirements

Python 3.7 or higher

# No external dependencies required
# Framework uses only Python standard library:
# - json (task loading)
# - pathlib (file operations)
# - zipfile (submission packaging)
# - typing (type hints)
# - collections (pattern detection)

# Optional for enhanced testing (not required):
# pytest>=7.0.0
